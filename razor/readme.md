A feeble attempt to run infinite arbitrary nested razor pages with .net core
# Requirement:
- as few build step as possible
- dynamic compile (or watched compile)
- source code stays with compiled binaries (preferably together)